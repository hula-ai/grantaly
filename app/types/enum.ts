export enum Role {
  USER = "user",
  ADMIN = "admin",
}
  
  // enums/Status.ts
  export enum Status {
    ACTIVE = "active",
    INACTIVE = "inactive",
  }
  export enum Mode {
    STUDENT = "student",
    TEACHER = "teacher",
  }